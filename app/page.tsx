export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", color: "#333" }}>
      <h1>Telegram Bot Service</h1>
      <p>The bot server is running.</p>
      <p>Webhook URL: <code>/api/webhook</code></p>
    </main>
  );
}
